R""(

# Examples

* Make the closure of the Hello package world-readable:

  ```console
  # nix store make-public -r nixpkgs#hello
  ```

# Description

This command makes the store paths specified by *installables* public,
i.e. world-readable.

)""
